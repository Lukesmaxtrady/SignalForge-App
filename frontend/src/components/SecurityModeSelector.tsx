import React from "react";

const SecurityModeSelector: React.FC = () => (
  <div style={{
    padding: "1rem",
    margin: "1rem 0",
    background: "#f1f5f9",
    borderRadius: "8px",
    textAlign: "center"
  }}>
    <strong>Security Mode Selector</strong>
    <div style={{ color: "#888", fontSize: "0.95em" }}>Feature coming soon.</div>
  </div>
);

export default SecurityModeSelector;
