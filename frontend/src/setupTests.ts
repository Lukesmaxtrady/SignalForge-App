// src/setupTests.ts

import '@testing-library/jest-dom';

// (Optional) Add more setup code here if needed

// Silence console errors for irrelevant warnings (use only if you get "not wrapped in act" warnings)
// beforeAll(() => {
//   jest.spyOn(console, 'error').mockImplementation((msg) => {
//     if (/Warning.*not wrapped in act/.test(msg)) return;
//     console.error(msg);
//   });
// });
